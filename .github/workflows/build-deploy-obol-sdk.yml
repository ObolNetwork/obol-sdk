name: Publish Obol-SDK to NPM

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          persist-credentials: false # Required to make GitHub forget this checkout for the next step

      - uses: actions/setup-node@v3
        with:
          node-version: '18.x'
          registry-url: 'https://registry.npmjs.org'

      - run: yarn install --frozen-lockfile

      - run: yarn workspace @obolnetwork/obol-sdk run ds:build

      - run: yarn workspace @obolnetwork/obol-sdk run generate-typedoc 

      - run: yarn workspace @obolnetwork/obol-sdk run ds:release:patch
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          HUSKY: 0
